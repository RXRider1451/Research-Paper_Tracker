<%= form_with(model: priority_list) do |form| %>
  <% if priority_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(priority_list.errors.count, "error") %> prohibited this priority_list from being saved:</h2>

      <ul>
        <% priority_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :priority_list, style: "display: block" %>
    <%= form.text_field :priority_list %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
